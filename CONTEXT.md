# CONTEXT — comic-troopers の共通言語

このファイルは Claude と共有する用語の辞書。実装中に出てくる言葉の定義をここへ集約する。

## ドメイン用語

- **ラン (run)** — 1回のプレイ。60秒固定。
- **ウェーブ (wave)** — 敵をまとめて湧かせる単位。経過時間で切り替わる。
- **ロックオン対象 (lockTarget)** — 自機から最も近く、かつ射程内にいる敵。毎フレーム再選択する。
- **コンボ** — 連続撃破数。被弾すると 0 に戻る。スコア倍率に効く。
- **ダッシュ** — 短距離の高速移動。発動中は無敵フレーム。クールダウンあり。
- **オノマトペ** — ヒット/撃破時に画面へ飛び出す日本語の効果音文字。コミック調演出の中核。

## コード上の約束

- 座標は **XZ 平面**（Y は上）。core が扱うのは `Vec2 = {x, z}` のみで、Y は描画側の都合。
- 単位は メートル / 秒。フレーム更新は `dt`（秒）を受け取る純粋関数に寄せる。
- `core/` は three.js を import しない。import したくなったら設計を疑う。
