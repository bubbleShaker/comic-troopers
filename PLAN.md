# comic-troopers 設計計画

## 何を作るか

three.js 製の、**スマホ縦持ちで1分で遊べる**コミック調 3D シューター。
GitHub Pages で URL を開いた瞬間に遊べることをゴールとする。

「E.X.トルーパーズ」らしさを、1分に収まる最小要素へ落とし込む:

| 原作の要素 | 本作での実装 |
| --- | --- |
| アメコミ調セルシェード | 3階調トゥーンシェーダ + 背面法アウトライン |
| オノマトペ演出 | ヒット/撃破時に日本語オノマトペをビルボード表示 |
| ロックオン射撃 | 最寄りの敵へ自動ロックオン、常時連射 |
| 高速ダッシュ回避 | 右側スワイプでダッシュ（無敵フレームあり） |
| 群れで襲うアカリッド | ウェーブで湧く虫型エネミー |

## 操作 / ルール（確定事項）

- 操作: 左下バーチャルスティックで移動のみ。射撃は自動ロックオン+常時連射。右側スワイプでダッシュ。
- ルール: 60秒スコアアタック。即死なし。被弾はコンボ切れ + スコア減点。
- 目的: 誰でも必ず1分遊べて、スコアがリトライ動機になること。

## アーキテクチャ

```
src/
  core/     ゲームロジック（three.js 非依存 / Node で単体テスト可能）
    types.ts      Vec2, Entity などの型
    world.ts      ワールド状態と1フレーム更新
    spawner.ts    ウェーブ定義と敵の湧き
    combat.ts     ロックオン選択・当たり判定・スコア
  render/   three.js 描画（core の状態を読むだけ）
    scene.ts      シーン/カメラ/ライト
    toon.ts       トゥーンマテリアルとアウトライン
    onomatopoeia.ts オノマトペのビルボード
  input/    入力（バーチャルスティック / スワイプ）
  ui/       HUD・リザルト（素の DOM）
  main.ts   組み立てとメインループ
```

方針:
- **core は three.js を import しない**。当たり判定は地面固定の 2D 円判定で済ませ、物理エンジンは入れない。
  1分ゲームに Rapier/Cannon は過剰で、スマホの初期ロードも重くなるため。
- **依存は three のみ**。ビルドは Vite、デプロイは GitHub Actions → Pages。
- 描画は core の状態を毎フレーム読んで反映するだけ。逆方向（描画→ロジック）の依存を作らない。

## マイルストーン

```mermaid
graph LR
  M0[M0 雛形とPagesデプロイ] --> M1[M1 自機移動とダッシュ]
  M1 --> M2[M2 敵とロックオン射撃]
  M2 --> M3[M3 コミック調ビジュアル]
  M2 --> M4[M4 60秒ルールとスコア]
```

- **M0** Vite + TypeScript + three の雛形、GitHub Actions で Pages 自動デプロイ。回転する箱が出れば合格。
- **M1** 地面・追従カメラ・自機、バーチャルスティック移動、スワイプダッシュ。
- **M2** 敵のウェーブ湧き、最寄り敵への自動ロックオン、弾と撃破判定。core の単体テストはここから。
- **M3** トゥーンシェーダ + アウトライン + オノマトペ + ヒットストップ。
- **M4** 60秒タイマー、スコア/コンボ、被弾ペナルティ、リザルトとリトライ。

## 非目標

- オンラインランキング、サウンド、複数ステージ、ボス戦、PC 向けの作り込み。
